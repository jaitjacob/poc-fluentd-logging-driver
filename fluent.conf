<source>
  @type forward
  @id forward_input
  bind 127.0.0.1
  port 24224
# Nest a <parse> block like below to tell the fluentd collector which parser to
# use
# <parse>
#   @type none
# </parse>
</source>

<match **>
  @type stdout
  @id stdout_output
</match>


